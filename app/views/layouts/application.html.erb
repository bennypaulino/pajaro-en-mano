<!DOCTYPE html>
<html>
<head>
  <title>Pajaro</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <header>
    <div class="col-xs-3">
      <%= link_to root_path, class: 'logo' do %>
        <div class="logo">
          <%= image_tag 'bird-in-hand-100px.jpg', class: 'img-responsive' %>
        </div>
      <% end %>
    </div>
    <div class="col-sm-4 hidden-xs slogan">
      Refránes queridas desde 2016
    </div>
    <div class="col-xs-8 col-xs-offset-1 col-sm-offset-0 col-sm-5 links">
      <!-- Button trigger modal -->
      <%= link_to 'Contribuir', new_quote_path, data: {toggle: 'modal', target: '#myModal'} %>
      &nbsp;&nbsp;&nbsp;
      <%= link_to 'Sobre', sobre_path %>
    </div>
  </header>


  <!-- Modal -->
  <div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
          <h4 class="modal-title" id="myModalLabel">Su Consejo...</h4>
        </div>
        <%= simple_form_for Quote.new do |f| %>
        <div class="modal-body">
          <%= f.input :saying, label: "Un dicho o refrán favorito...", placeholder: "Más vale un pájaro en mano que ciento volando." %>
          <br>
          <%= f.input :author, label: "Quién es el que le encanta decir esto?", placeholder: "Fulano de la vuelta abajo, o su nombre" %>
          <br>
          <%= f.input :country, label: "País de origen..." %>
          <br>
          
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-default" data-dismiss="modal">Cerrar</button>
          <%= f.submit 'Crear', class: 'btn btn-success' %>
        </div>
        <% end %>  
      </div>
    </div>
  </div>




  <hr />
  <br class="clear" />

<div class="container">
  <% if flash[:error].present? %>
    <div class="col-xs-10 col-xs-offset-1 alert alert-danger">
      <%= flash[:error].html_safe %>
    </div>
  <% end %>
<%= yield %>
</div>

<div class="footer">
  <div class="container">
    <div class="col-xs-12 col-sm-6">
      <%= link_to "Otro más!", root_path, class: 'next-quote' %>
    </div>
    <div class="col-xs-12 col-sm-6 footer-blurb">
      Este sitio ha sido creado con mucho orgullo y amor en Orlando, FL departe de <a href="http://www.iseeincode.com">Benny Paulino</a>.
    </div>
  </div>
</div>


</body>
</html>
